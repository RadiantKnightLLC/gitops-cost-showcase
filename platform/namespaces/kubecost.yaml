apiVersion: v1
kind: Namespace
metadata:
  name: kubecost
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
  labels:
    environment: platform
    team: platform
    cost-center: shared-overhead
    owner: platform-team
    kubernetes.io/metadata.name: kubecost
