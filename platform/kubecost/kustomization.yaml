apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - budget-alerts.yaml
  # Note: kubecost-rendered.yaml is applied separately via kubectl
  # due to Kustomize label transformation conflicts with Helm-generated labels
  # Apply with: kubectl apply -f base/kubecost-rendered.yaml
